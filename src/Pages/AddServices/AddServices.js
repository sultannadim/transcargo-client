import React from "react";
import useTitle from "../../hooks/useTitle";

const AddServices = () => {
  useTitle("Add Services");
  return (
    <div>
      <h1>Add services page</h1>
    </div>
  );
};

export default AddServices;
